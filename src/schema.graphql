type Query {
  info: String!
  feed: Feed!
}

type Feed {
  register: [Register!]!
}

type Mutation {
  register(
    email: String!
    password: String!
    name: String!
    phone: Int
  ): AuthPayload
}

type AuthPayload {
  token: String
  user: Register
}

type Register {
  user_id: ID!
  name: String!
  email: String!
  password: String!
  phone: Int!
}
