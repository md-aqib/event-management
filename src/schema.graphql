type Query {
  info: String!
  feed: [User!]!
}

type Mutation {
  post(url: String!, description: String!): Link!
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  phone: String!
}
